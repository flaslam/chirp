import React from "react";

// TODO: this wil contain all functions that need to be passed down to both post and postMain,
// so they can inherit from this
const PostContainer = () => {
  return <div>PostContainer</div>;
};

export default PostContainer;
